{
  "name": "dandelion",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "SonyPradana",
  "type": "module",
  "scripts": {
    "lint": "oxlint . --deny-warnings",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "clean": "rimraf dist",
    "copy:static": "node scripts/copy-static.js",
    "compile": "rolldown -c",
    "manifest": "node scripts/build-manifest.js",
    "build:chrome": "pnpm clean && pnpm compile && pnpm copy:static && pnpm manifest",
    "build:firefox": "node scripts/build-firefox.js",
    "build": "pnpm build:chrome && pnpm build:firefox",
    "release:chrome": "pnpm build:chrome && node scripts/package.js chrome",
    "release:firefox": "pnpm build:firefox && node scripts/package.js firefox",
    "release": "pnpm build && node scripts/package.js all"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.63.0",
    "rimraf": "^6.1.3",
    "rolldown": "^1.0.0",
    "web-ext": "^9.4.0"
  }
}
