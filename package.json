{
  "name": "dandelion",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "build": "rimraf dist && rollup -c && copyfiles -u 1 src/view/* dist && copyfiles -u 1 icons/* dist/icons && node scripts/build-manifest.js"
  },
  "keywords": [],
  "author": "SonyPradana",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "dotenv": "^17.2.3",
    "eslint": "^9.38.0",
    "globals": "^16.4.0",
    "neostandard": "^0.12.2",
    "rimraf": "^6.1.0",
    "rollup": "^4.52.5"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
