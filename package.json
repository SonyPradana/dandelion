{
  "name": "dandelion",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "clean": "rimraf dist",
    "copy:static": "copyfiles -u 1 src/view/{popup.html,popup.css} dist/ && copyfiles -u 1 icons/* dist/icons",
    "rollup": "rollup -c",
    "manifest": "node scripts/build-manifest.js",
    "build": "pnpm clean && pnpm rollup && pnpm copy:static && pnpm manifest",
    "start:firefox": "pnpm build && web-ext run --source-dir dist"
  },
  "keywords": [],
  "author": "SonyPradana",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.9",
    "@rollup/plugin-node-resolve": "^15.3.1",
    "copyfiles": "^2.4.1",
    "dotenv": "^17.2.3",
    "eslint": "^9.38.0",
    "globals": "^16.4.0",
    "neostandard": "^0.12.2",
    "rimraf": "^6.1.0",
    "rollup": "^4.52.5",
    "web-ext": "^9.1.0"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
